TO COMPILE:
'make'

TO RUN:
./trace-cuda > out.ppm
convert to jpg by calling:
ppmtojpeg out.ppm > out.jpg

SEQUENTIAL PROGRAM:
under the sequential/ directory
- compile with 'make'
- run with './trace-seq > out.ppm'
- when finished, convert to jpg by calling 'ppmtojpeg out.ppm > out.jpeg'